<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.tliaswebmanagement.mapper.EmpMapper">
    <!-- 更新员工 -->
    <update id="update">
        update emp
        <set>
            <if test="username != null and username != ''"> username = #{username}, </if>
            <if test="password != null and password != ''"> password = #{password}, </if>
            <if test="name != null and name != ''"> name = #{name}, </if>
            <if test="gender != null"> gender = #{gender}, </if>
            <if test="image != null and image != ''"> image = #{image}, </if>
            <if test="job != null"> job = #{job}, </if>
            <if test="entrydate != null"> entrydate = #{entrydate}, </if>
            <if test="deptId != null"> dept_id = #{deptId}, </if>
            <if test="updateTime != null"> update_time = #{updateTime} </if>
        </set>
        where id = #{id}
    </update>
    <!--
        批量删除员工
        <foreach> 标签：遍历集合，生成逗号分隔的 ID 列表
          collection="ids"  → Java 方法参数名（List<Integer> ids）
          item="id"         → 每次遍历的临时变量名
          separator=","     → 元素之间的分隔符
          open="("          → 遍历开始前拼接的字符
          close=")"         → 遍历结束后拼接的字符
        最终生成：delete from emp where id in (1, 2, 3)
    -->
    <delete id="delete">
        delete
        from emp
        where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <!--
        动态条件查询员工列表
        与 EmpMapper.list(name, gender, begin, end) 方法绑定
        <where> 标签：自动处理 WHERE 关键字，会智能去除多余的 AND/OR
        <if> 标签：当参数不为 null 且不为空时，才拼接对应的条件
    -->
    <select id="list" resultType="org.example.tliaswebmanagement.pojo.Emp">
        select * from emp
        <where>
            <if test="name != null and name != ''">
                name like concat('%', #{name}, '%')
            </if>
            <if test="gender != null">
                and gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                and entrydate between #{begin} and #{end}
            </if>
        </where>
        order by update_time desc
    </select>

</mapper>
